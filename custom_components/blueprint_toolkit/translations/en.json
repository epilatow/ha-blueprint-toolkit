{
  "config": {
    "step": {
      "user": {
        "title": "Blueprint Toolkit",
        "description": "Set up the integration. Click Submit to install the bundled blueprints and rendered docs into your config directory."
      }
    },
    "abort": {
      "single_instance_allowed": "Only one instance of Blueprint Toolkit can be configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Blueprint Toolkit Options",
        "data": {
          "cli_symlink_dir": "CLI symlink directory"
        },
        "data_description": {
          "cli_symlink_dir": "Bundled CLI scripts (zwave_network_info.py) will be symlinked into this directory. Path must be writable by Home Assistant. On HAOS / Supervised, the integration runs in its own container -- use a shared-mount path like /config (paths like /root resolve inside HA's container, not the SSH add-on). Leave empty to skip."
        }
      }
    }
  },
  "issues": {
    "install_conflicts": {
      "title": "Install conflicts",
      "description": "The integration found existing files or symlinks at one or more destinations it wants to install to. Click Submit to remove and replace them, or Ignore if you'd like to keep what's there.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Overwrite conflicting paths?",
            "description": "The following paths will be removed and replaced with symlinks pointing into the integration's bundled payload:\n\n{conflicts}\n\nDirectories listed above will not be removed automatically; move them aside before retrying."
          }
        }
      }
    },
    "install_failure": {
      "title": "Install failure",
      "description": "The integration could not complete its install. Fix the underlying problem on disk, then click Submit to retry.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Retry install?",
            "description": "The previous install reported these errors:\n\n{errors}\n\nClick Submit to re-run the install."
          }
        }
      }
    },
    "edw_device_entity_id_drift": {
      "title": "Entity ID drift on device: {device_name}",
      "description": "Device `{device_name}` has {count} entities whose entity IDs have drifted from their defaults. Click Submit to regenerate them.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Regenerate entity IDs?",
            "description": "{attribution}\n\nRegenerate drifted entity IDs on `{device_name}` ({count} entities):\n\n{entities}\n\nOnly the entities the last scan flagged are changed; anything you've added to the watchdog's exclusions was already left out.\n\nClick Submit to apply."
          }
        }
      }
    },
    "edw_device_entity_name_drift": {
      "title": "Entity name drift on device: {device_name}",
      "description": "Device `{device_name}` has {count} entities whose names have drifted from their defaults. Click Submit to fix them.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Update entity names?",
            "description": "{attribution}\n\nUpdate {count} drifted entity name(s) on `{device_name}`:\n\n{entities}\n\nA cleared override reverts to the integration default name. Only the entities the last scan flagged are changed; anything you've added to the watchdog's exclusions was already left out.\n\nClick Submit to apply."
          }
        }
      }
    },
    "dw_device_disabled_diagnostics": {
      "title": "Disabled diagnostic entities on device: {device_name}",
      "description": "Device `{device_name}` has {count} recommended diagnostic entities that are currently disabled. Click Submit to re-enable all of them.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Re-enable diagnostic entities?",
            "description": "{attribution}\n\nRe-enable the {count} disabled recommended-diagnostic entities on `{device_name}`:\n\n{entities}\n\nOnly the entities the last scan flagged are changed; anything you've added to the watchdog's exclusions was already left out.\n\nClick Submit to apply."
          }
        }
      }
    },
    "edw_visible_aliased_entity": {
      "title": "Visible aliased entity: {source_entity_id}",
      "description": "`{source_entity_id}` is wrapped by switch_as_x but its source entity is still visible, so both rows show up in dashboards, voice assistants, and entity pickers. Click Submit to re-hide the source.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Re-hide {source_entity_id}?",
            "description": "{attribution}\n\nRe-hide the source entity (sets hidden_by=integration), restoring the original switch_as_x behaviour:\n\n{entities}\n\nClick Submit to apply."
          }
        }
      }
    }
  }
}
